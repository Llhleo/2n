# 2n 网站 v20

本版在 v19 基础上重做开场与进入花园的连续衔接，保留旧版，未上传或覆盖线上网站。

## 这版改了什么

- 加载五张背景后播放约 5.6 秒开场：两行标语依次升起、幕布退场、分层背景展开、黑色 2ⁿ 从前景后升起，最后显示主文案和操作按钮。
- 开场期间锁定滚动。支持跳过、重播；正常结束后交还滚动控制。
- 首次滑动先推进开场镜头并融入花园，再进入五生态连续横向浏览。背景和文字有独立的运动速度。
- 手机保留横向叙事，管理层为介绍页 + 四位成员，每位一个屏宽；增加左右方向手势映射、章节按钮和键盘控制。
- 五张背景保持原有文件；开场纹理按比例重复绘制，不再在窄屏上纵向拉长。
- 默认仍是黑色文字标徽，候选图片标徽保留。CNFlyDream、sschara 全名和既有贡献说明继续使用。
- 成员资料移入静态 HTML，即使动画脚本加载失败也不会丢失。背景加载有时限，启动失败有 12 秒解锁兜底。
- 提供“减少动态”开关，并尊重系统偏好。原有完整纪念视频保留，点击播放才加载。

## 你现在要做的

1. 解压整个文件夹，不要只取出 HTML。电脑上打开 `dist/index.html` 查看，或交给现有网站维护者预览 `dist/`。
2. 先验收三个位置：开场 2ⁿ 的遮挡比例；从开场滑入花园是否自然；手机管理层横向阅读是否舒服。
3. 用 iPhone 的正常网页浏览器测试上下滑动、左右滑动、旋转屏幕和后台返回。发一段包含问题前后操作的录屏，比单张截图更容易调整动效。
4. 标徽不必现在定稿。如果决定用压缩包候选标徽，将 `dist/app.js` 顶部 `BRAND_MODE` 从 `wordmark` 改为 `image`。
5. 视觉与真机手感确认后，再更新线上网站。这轮没有操作 GitHub 或线上发布。

## 文件说明

- `dist/`：完整静态网站，可直接作为网站发布目录。
- `dist/index.html`：文字内容、成员全名、贡献资料。
- `dist/style.css`：排版、遮挡层级、手机样式。
- `dist/motion.js`：开场分镜的时间参数。
- `dist/app.js`：背景分层、滚动和交互。
- `dist/assets/`：五生态背景、两种图标素材及原有完整视频。
- `tools/`：可重复运行的本地检查和小屏验收页面，不需要上传到正式网站。

## 验收范围与限制

已检查 JavaScript 语法、资源路径、五生态与四位管理者、开场分段时间、进入镜头与横向轨道的边界；已实际查看桌面开场、锁滚动、花园/沙漠切换、管理层导航、小屏管理层与减少动态模式；主脚本缺失后可解锁并继续浏览。

小屏为 Chrome 内的尺寸模拟，并非 iOS Safari 真机。真实触控惯性、Safari 地址栏伸缩及后台恢复仍需真机验收；不能将这轮检查视为已经通过。

这版补齐的是分层运动与节奏基础，还不是 Bienville 的一比一复刻。当前素材仍是二维游戏背景，不具备其完整三维场景的空间细节；后续如需更近的空间感，应在确定开场分镜后准备可分离的前景、中景、远景素材，而不是继续叠加淡入动画。

## 本地维护检查

已安装 Node.js 时，在项目文件夹运行：

```sh
node tools/check-site.mjs
node --test tools/motion.test.cjs
```

不依赖外部动画 CDN，也不需要安装第三方依赖。检查脚本不包含部署或外部写入。
